extends layout 
block content 
    #main_content
        #topBar
            form(action="/logout", method="post")
                input(
                type = "submit"
                value = "Logout"
            )
        #chart
        #sidebar SIDEBAR
            select#empresas
        #news NOTICIAS
append scripts
    script(src="https://cdn.jsdelivr.net/npm/apexcharts")
    script(src="js/func.js")