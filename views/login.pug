extends layout
block content 
    #main_content
        .text EMAIL
        form(action="/login", method="post") 
            input(
                class = "email"
                type = "email"
                name = "email"
            )
            .text PASSWORD
            input(
                class = "password"
                type = "password"
                name = "password"
            )
            br
            input(
                type = "submit"
                value = "Login"
            )